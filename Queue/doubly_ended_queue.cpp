#include<bits/stdc++.h>
using namespace std;
int main()
{
    deque<int> qt;
    qt.push_back(23);
    qt.push_back(45);
    qt.push_front(2);
    qt.pop_back();
   cout<< qt.empty();
    // qt.empty();

return 0;
} 